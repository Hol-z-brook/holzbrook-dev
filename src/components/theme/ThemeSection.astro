---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<section class={`theme-section rounded-2xl border border-gray-200 bg-white p-8 shadow-sm hover:border-primary-200 transition-colors duration-150 ease-in-out ${className}`}>
  <slot />
</section>

<style>
  /* Removed theme function styles in favor of Tailwind classes */
</style> 