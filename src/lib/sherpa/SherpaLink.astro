---
import { Link } from '../ui';

interface Props {
  class?: string;
  href?: string;
}

const { class: className = '', href }: Props = Astro.props;
---

<Link href={href} class={className}>
  <slot />
</Link>
