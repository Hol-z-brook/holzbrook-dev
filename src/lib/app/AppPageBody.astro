---
import UI from '../../lib/ui';

interface Props {
  class?: string;
  backgroundColor?: string;
}

const { class: className, backgroundColor = 'bg-white' }: Props = Astro.props;
---

<UI.Layout.Box class={`${backgroundColor} ${className}`}>
  <slot />
</UI.Layout.Box>
