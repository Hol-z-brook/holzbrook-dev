---
interface Props {
  class?: string;
  'data-testid'?: string;
}

const { class: className, 'data-testid': dataTestId }: Props = Astro.props;
---

<code class={`font-mono bg-gray-100 px-1 rounded ${className}`} data-testid={dataTestId}>
  <slot />
</code>
