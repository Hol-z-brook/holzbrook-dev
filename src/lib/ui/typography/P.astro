---
interface Props {
  class?: string;
  'data-testid'?: string;
}

const { class: className, 'data-testid': dataTestId }: Props = Astro.props;
---

<p class={`${className}`} data-testid={dataTestId}>
  <slot />
</p>
