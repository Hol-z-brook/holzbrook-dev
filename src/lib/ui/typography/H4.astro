---
interface Props {
  class?: string;
  'data-testid'?: string;
}

const { class: className, 'data-testid': dataTestId }: Props = Astro.props;
---

<h4 class={`text-xl font-bold ${className}`} data-testid={dataTestId}>
  <slot />
</h4>
