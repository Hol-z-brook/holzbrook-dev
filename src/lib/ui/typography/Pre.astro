---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<pre
  class={`bg-gray-100 p-4 rounded-lg font-mono text-sm overflow-x-auto ${className}`}>
  <slot />
</pre>
