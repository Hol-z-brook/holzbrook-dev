---
interface Props {
  class?: string;
  'data-testid'?: string;
}

const { class: className, 'data-testid': dataTestId }: Props = Astro.props;
---

<blockquote class={`border-l-4 border-gray-300 pl-4 ${className}`} data-testid={dataTestId}>
  <slot />
</blockquote>
