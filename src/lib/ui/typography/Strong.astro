---
interface Props {
  class?: string;
  'data-testid'?: string;
}

const { class: className, 'data-testid': dataTestId }: Props = Astro.props;
---

<strong class={`font-semibold ${className}`} data-testid={dataTestId}>
  <slot />
</strong>
