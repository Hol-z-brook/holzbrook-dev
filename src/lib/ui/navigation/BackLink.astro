---
interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a {href} class={`text-primary-600 hover:text-primary-700 inline-block ${className || ''}`}>
  &larr; <slot />
</a>
