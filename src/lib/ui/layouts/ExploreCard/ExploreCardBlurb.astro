---
import { P } from '../../typography';

interface Props {
  class?: string;
  'data-testid'?: string;
}

const { class: className, 'data-testid': dataTestId }: Props = Astro.props;
---

<p class={`text-lg mb-6 ${className}`} data-testid={dataTestId}>
  <slot />
</p>
