---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
}

const { as: Tag = 'div', class: className }: Props = Astro.props;
---

<Tag class={`flex items-center justify-center ${className}`}>
  <slot />
</Tag>
