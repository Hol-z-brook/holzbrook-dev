---
interface Props {
  class?: string;
}

const {
  class: className,
} = Astro.props;
---

<nav class={`max-w-4xl mx-auto mt-4 text-sm text-gray-600 ${className}`}>
  <ul class="flex space-x-2">
    <slot />
  </ul>
</nav>
