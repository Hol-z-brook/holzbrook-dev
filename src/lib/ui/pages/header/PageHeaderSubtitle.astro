---
interface Props {
  class?: string;
  'data-testid'?: string;
}

const { class: className, 'data-testid': dataTestId } = Astro.props;
---

<div
  class={`text-xl font-medium text-primary-600 mb-6 ${className || ''}`}
  data-testid={dataTestId}
>
  <slot />
</div>
