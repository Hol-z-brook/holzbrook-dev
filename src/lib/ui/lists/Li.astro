---
interface Props {
  class?: string;
  'data-testid'?: string;
}

const { class: className = '', 'data-testid': dataTestId } = Astro.props;
---

<li class={`text-gray-600 ${className}`} data-testid={dataTestId}>
  <slot />
</li>
