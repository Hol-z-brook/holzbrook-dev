---
interface Props {
  class?: string;
  'data-testid'?: string;
}

const { class: className = '', 'data-testid': dataTestId } = Astro.props;
---

<ol class={`list-decimal list-inside space-y-2 ${className}`} data-testid={dataTestId}>
  <slot />
</ol>
