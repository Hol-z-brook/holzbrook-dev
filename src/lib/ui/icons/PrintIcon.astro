---
import type { IconProps } from './IconProps';

/**
 * Print icon component that can be used to indicate print actions or functionality.
 * Supports both filled and outlined styles, custom sizing, and colors.
 */
interface Props extends IconProps {}

const {
  class: className = '',
  size = 5,
  color = 'currentColor',
  filled = true,
}: Props = Astro.props;
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  class={`h-${size} w-${size} ${className}`}
  viewBox="0 0 20 20"
  fill={filled ? color : 'none'}
  stroke={!filled ? color : undefined}
>
  <path
    d="M6 2a2 2 0 00-2 2v3h12V4a2 2 0 00-2-2H6zM4 9a2 2 0 00-2 2v3a2 2 0 002 2h1v2a1 1 0 001 1h8a1 1 0 001-1v-2h1a2 2 0 002-2v-3a2 2 0 00-2-2H4zm3 8v-3h6v3H7z"
  ></path>
</svg>

