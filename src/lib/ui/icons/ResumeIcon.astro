---
import { Icon } from 'astro-icon/components';

/**
 * Resume icon component that can be used to indicate resume downloads or viewing.
 * Supports both filled and outlined styles, custom sizing, and colors.
 */
interface Props {
  class?: string;
  size?: number;
  color?: string;
}

const { class: className, size, color } = Astro.props;
---

<Icon name="local:file-document" class={className} size={size} color={color} />
