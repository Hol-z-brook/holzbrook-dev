---
import { Icon } from 'astro-icon/components';

/**
 * LinkedIn icon component that can be used to link to LinkedIn profiles.
 * Supports both filled and outlined styles, custom sizing, and colors.
 */
interface Props {
  class?: string;
  size?: number;
  color?: string;
}

const { class: className, size, color } = Astro.props;
---

<Icon name="local:linkedin" class={className} size={size} color={color} />
