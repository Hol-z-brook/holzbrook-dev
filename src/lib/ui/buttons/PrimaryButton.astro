---
import Button from './Button.astro';

interface Props {
  radius?: string;
  class?: string;
}

const { radius, class: className } = Astro.props;
---

<Button variant="primary" radius={radius} class={className}>
  <slot />
</Button>
