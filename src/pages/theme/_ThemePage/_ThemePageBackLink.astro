---
import { makeSherpa } from '../../../lib/sherpa/Sherpa';
import UI from '../../../lib/ui';

const sherpa = makeSherpa();
---

<UI.Nav.BackLink href={sherpa.theme.index()} class="mb-4">Back to Pickles UI</UI.Nav.BackLink> 