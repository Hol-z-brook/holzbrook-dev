---
import Box from "../../../lib/ui/Box.astro";

interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<Box class={`px-4 py-2 text-gray-900 font-medium ${className}`}>
  <slot />
</Box> 