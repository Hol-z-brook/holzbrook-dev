---
import { Box } from '../../../../lib/ui';

interface Props {
  class?: string;
  id?: string;
}

const { class: className, id } = Astro.props;
---

<Box {id} class:list={['space-y-12', className]}>
  <slot />
</Box>
