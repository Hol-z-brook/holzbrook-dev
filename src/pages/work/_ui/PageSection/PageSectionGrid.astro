---
import { Box } from '../../../../lib/ui';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<Box class:list={['grid grid-cols-1 lg:grid-cols-2 gap-8', className]}>
  <slot />
</Box>
