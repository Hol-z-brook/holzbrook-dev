---
import UI from "../../../../lib/ui";

interface Props {
  class?: string;
  id?: string;
}

const { class: className, id } = Astro.props;
---

<UI.Layout.Box {id} class:list={["space-y-12", className]}>
  <slot />
</UI.Layout.Box> 