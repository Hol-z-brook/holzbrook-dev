---
import { LandingPage } from '@/lib/ui/layouts';

interface Props {
  class?: string;
  title?: string;
}

const { class: className, title } = Astro.props;
---

<LandingPage.Container title={title} class:list={['bg-gray-50', className]}>
  <LandingPage.Header />
  <slot />
</LandingPage.Container>
