---
import UI from "../../../../lib/ui";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<UI.Typography.H2 class:list={["text-2xl font-medium text-gray-900", className]}>
  <slot />
</UI.Typography.H2> 