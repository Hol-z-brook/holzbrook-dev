---
import UI from '../../../lib/ui';

interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<UI.Typography.Text.P {...rest} class={`text-lg mb-8 ${className ?? ''}`}>
  <slot />
</UI.Typography.Text.P>
