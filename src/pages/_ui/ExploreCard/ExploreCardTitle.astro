---
import UI from "../../../lib/ui";

interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<UI.Typography.H2 {...rest} class={`text-3xl mb-4 ${className ?? ''}`}>
  <slot />
</UI.Typography.H2> 