---
import { ExploreCard, PageHero } from '../lib/ui';
import App from '../lib/app';
import { pageData } from './_/data';
---

<App.Page>
  <App.PageBody backgroundColor="bg-gray-100" class="flex flex-col min-h-screen">
    <PageHero title={pageData.title} subtitle={pageData.subtitle} />

    <section class="py-12">
      <div class="max-w-6xl mx-auto px-6">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
          <ExploreCard.Container bgColor={pageData.sections.work.bgColor}>
            <ExploreCard.Title>
              {pageData.sections.work.title}
            </ExploreCard.Title>
            <ExploreCard.Blurb>
              {pageData.sections.work.description}
            </ExploreCard.Blurb>
            <ExploreCard.Cta
              href={pageData.sections.work.link}
              class={pageData.sections.work.ctaClass}
            >
              {pageData.sections.work.cta}
            </ExploreCard.Cta>
          </ExploreCard.Container>

          <ExploreCard.Container bgColor={pageData.sections.family.bgColor}>
            <ExploreCard.Title>
              {pageData.sections.family.title}
            </ExploreCard.Title>
            <ExploreCard.Blurb>
              {pageData.sections.family.description}
            </ExploreCard.Blurb>
            <ExploreCard.Cta
              href={pageData.sections.family.link}
              class={pageData.sections.family.ctaClass}
            >
              {pageData.sections.family.cta}
            </ExploreCard.Cta>
          </ExploreCard.Container>

          <ExploreCard.Container bgColor={pageData.sections.play.bgColor}>
            <ExploreCard.Title>
              {pageData.sections.play.title}
            </ExploreCard.Title>
            <ExploreCard.Blurb>
              {pageData.sections.play.description}
            </ExploreCard.Blurb>
            <ExploreCard.Cta
              href={pageData.sections.play.link}
              class={pageData.sections.play.ctaClass}
            >
              {pageData.sections.play.cta}
            </ExploreCard.Cta>
          </ExploreCard.Container>
        </div>
      </div>
    </section>

    <App.PageFooter />
  </App.PageBody>
</App.Page>
